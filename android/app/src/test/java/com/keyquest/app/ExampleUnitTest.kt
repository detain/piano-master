package com.keyquest.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Trivial smoke test so `./gradlew testDebugUnitTest` has something green
 * from day one. Real logic tests arrive with the first domain code.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}